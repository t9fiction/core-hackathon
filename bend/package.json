{
  "name": "pumpfun-hardhat",
  "version": "1.0.0",
  "description": "PumpFun meme token factory with anti-rug pull features",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "test:token": "npx hardhat test test/PumpFunToken.test.js",
    "test:factory": "npx hardhat test test/PumpFunFactoryLite.test.js",
    "test:integration": "npx hardhat test test/Integration.test.js",
    "test:coverage": "npx hardhat coverage",
    "deploy:sepolia": "npx hardhat run scripts/deploy.js --network sepolia",
    "deploy:base": "npx hardhat run scripts/deploy.js --network base",
    "clean": "npx hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.0.0",
    "hardhat": "^2.25.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0",
    "dotenv": "^17.0.1"
  }
}
